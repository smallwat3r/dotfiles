#!/usr/bin/env bash
# Enable Emacs daemon to start automatically at login.
# The plist is stowed from macos/_root to /Library/LaunchAgents.
# -w clears any disabled override left by a previous deinit.
exec launchctl load -w /Library/LaunchAgents/gnu.emacs.daemon.plist
