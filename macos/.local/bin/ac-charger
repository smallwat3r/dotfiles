#!/usr/bin/env bash
# Check if the Mac is connected to AC power.
# Prints "on" if charging, "off" otherwise.
# Useful for status bars like tmux.

if [[ $(pmset -g ac) != "No adapter attached." ]]; then
  echo "on"
else
  echo "off"
fi
