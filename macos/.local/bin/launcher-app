#!/usr/bin/env bash
# Launch macOS applications using fzf.
# Uses Spotlight index for fast lookups.
set -euo pipefail

. "${HOME}/.local/lib/launcher.sh"

require fzf

target=$(mdfind "kMDItemKind == 'Application'" 2>/dev/null | fzf_pick) || exit 0

open "$target"
