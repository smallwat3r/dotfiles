#!/usr/bin/env bash
# Shorten pwd

_shpwd=$(
  echo ~+ |
    sed "s;$HOME;~;" |
    sed 's;\(/.\)[^/]*;\1;g' |
    sed 's/.$//'
)

if [[ $PWD == $HOME ]]; then
  echo '~'
else
  echo "$_shpwd${PWD##*/}"
fi
