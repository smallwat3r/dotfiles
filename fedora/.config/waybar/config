{
    "layer": "top",
    "position": "top",
    "height": 14,
    "spacing": 0,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": [],
    "modules-right": [
        "custom/sysinfo",
        "custom/sep",
        "tray",
        "custom/sep",
        "custom/power-profile",
        "battery",
        "custom/sep",
        "clock",
        "custom/recording"
    ],

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}"
    },

    "sway/mode": {
        "format": "{}"
    },

    "custom/sysinfo": {
        "exec": "~/.local/bin/sysinfo",
        "return-type": "json",
        "interval": 5
    },

    "tray": {
        "icon-size": 14,
        "spacing": 4
    },

    "custom/sep": {
        "format": "·"
    },

    "custom/recording": {
        "exec": "~/.local/bin/recording-ctl status",
        "exec-on-event": true,
        "interval": 1,
        "format": "{}",
        "on-click": "~/.local/bin/recording-ctl stop",
        "tooltip": false
    },

    "network": {
        "format-wifi": "W:{essid}",
        "format-ethernet": "E:{ipaddr}",
        "format-disconnected": "W-",
        "on-click": "nm-connection-editor",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}"
    },

    "wireplumber": {
        "format": "V{volume}%",
        "format-muted": "V-",
        "on-click": "pavucontrol",
        "on-click-right": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
        "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"
    },

    "custom/power-profile": {
        "exec": "power-profile-ctl status",
        "exec-on-event": true,
        "interval": 5,
        "format": "{}",
        "on-click": "power-profile-ctl cycle",
        "tooltip": true,
        "tooltip-format": "Power profile: {}"
    },

    "battery": {
        "interval": 30,
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}%",
        "format-charging": "{capacity}%+",
        "format-plugged": "{capacity}%+",
        "on-click": "gnome-power-statistics || true"
    },

    "clock": {
        "interval": 60,
        "format": "{:%a %d %b %H:%M}",
        "on-click": "gnome-calendar || true",
        "tooltip-format": "<tt>{calendar}</tt>"
    }
}
