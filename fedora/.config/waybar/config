{
    "layer": "top",
    "position": "top",
    "height": 14,
    "spacing": 0,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": [],
    "modules-right": [
        "cpu",
        "memory",
        "tray",
        "bluetooth",
        "network",
        "wireplumber",
        "custom/power-profile",
        "battery",
        "clock"
    ],

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}"
    },

    "sway/mode": {
        "format": "{}"
    },

    "cpu": {
        "interval": 5,
        "format": "C{usage}% ·",
        "on-click": "foot --title=floating htop"
    },

    "memory": {
        "interval": 5,
        "format": "M{percentage}% ·",
        "on-click": "foot --title=floating htop"
    },

    "bluetooth": {
        "format": "B ·",
        "format-connected": "B:{device_alias} ·",
        "format-disabled": "B- ·",
        "format-off": "B- ·",
        "on-click": "blueman-manager",
        "tooltip-format": "{controller_alias}\n{status}"
    },

    "tray": {
        "icon-size": 14,
        "spacing": 4
    },

    "network": {
        "format-wifi": "W:{essid} ·",
        "format-ethernet": "E:{ipaddr} ·",
        "format-disconnected": "W- ·",
        "on-click": "nm-connection-editor",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}"
    },

    "wireplumber": {
        "format": "V{volume}% ·",
        "format-muted": "V- ·",
        "on-click": "pavucontrol",
        "on-click-right": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
        "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
        "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"
    },

    "custom/power-profile": {
        "exec": "p=$(tuned-adm active 2>/dev/null | sed 's/Current active profile: //'); case $p in powersave) echo ECO;; balanced) echo BAL;; throughput-performance) echo PERF;; *) echo $p;; esac",
        "exec-on-event": true,
        "format": "{} ·",
        "on-click": "p=$(tuned-adm active 2>/dev/null | sed 's/Current active profile: //'); case $p in powersave) tuned-adm profile balanced;; balanced) tuned-adm profile throughput-performance;; *) tuned-adm profile powersave;; esac",
        "tooltip": true,
        "tooltip-format": "Power profile: {}"
    },

    "battery": {
        "interval": 30,
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% ·",
        "format-charging": "{capacity}%+ ·",
        "format-plugged": "{capacity}%+ ·",
        "on-click": "gnome-power-statistics || foot --title=floating sh -c 'upower -d | less'"
    },

    "clock": {
        "interval": 60,
        "format": "{:%a %d %b %H:%M}",
        "on-click": "gnome-calendar || true",
        "tooltip-format": "<tt>{calendar}</tt>"
    }
}
