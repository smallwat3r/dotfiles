{
    "layer": "top",
    "position": "top",
    "height": 14,
    "spacing": 0,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": [],
    "modules-right": [
        "custom/sysinfo",
        "custom/sep",
        "custom/yubikey",
        "tray",
        "custom/sep",
        "custom/power-profile",
        "battery",
        "custom/sep",
        "clock",
        "custom/recording"
    ],

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{name}"
    },

    "sway/mode": {
        "format": "{}"
    },

    "custom/sysinfo": {
        "exec": "~/.local/bin/sysinfo",
        "return-type": "json",
        "interval": 5
    },

    "tray": {
        "icon-size": 14,
        "spacing": 4
    },

    "custom/sep": {
        "format": "·",
        "tooltip": false
    },

    "custom/yubikey": {
        "exec": "grep -qrs 1050 /sys/bus/usb/devices/*/idVendor && echo 'YK' || echo ''",
        "interval": 30,
        "tooltip": false
    },

    "custom/recording": {
        "exec": "pgrep -x wf-recorder >/dev/null && echo 'REC' || echo ''",
        "interval": 5,
        "format": "{}",
        "on-click": "pkill -SIGINT wf-recorder",
        "tooltip": false
    },

    "custom/power-profile": {
        "exec": "power-profile-ctl status",
        "exec-on-event": true,
        "interval": 5,
        "format": "{}",
        "on-click": "power-profile-ctl cycle",
        "tooltip": true,
        "tooltip-format": "Power profile: {}"
    },

    "battery": {
        "interval": 30,
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}%",
        "format-charging": "{capacity}%+",
        "format-plugged": "{capacity}%+",
        "on-click": "gnome-power-statistics || true"
    },

    "clock": {
        "interval": 60,
        "format": "{:%a %d %b %H:%M}",
        "on-click": "gnome-calendar || true",
        "tooltip-format": "<tt>{calendar}</tt>"
    }
}
