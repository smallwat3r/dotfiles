* smallwat3r's dotfiles

Personal dotfiles managed with [[https://www.gnu.org/software/stow/][GNU Stow]].

Feel free to reuse any part of this repository, but note that it is tailored to my own workflow and updated frequently. Back up your existing setup before using the provided Makefile.

** Requirements

- [[https://www.gnu.org/software/stow/][GNU Stow]]

** Installation

Clone into your home directory:

#+begin_src shell
git clone https://github.com/smallwat3r/dotfiles.git ~/dotfiles
#+end_src

Symlink all dotfiles (the Makefile detects the OS and selects the appropriate targets):

#+begin_src shell
cd ~/dotfiles && make symlink
#+end_src

List available commands:

#+begin_src shell
make help
#+end_src

** Structure

These configurations support multiple platforms:

| Directory        | Description                                          |
|------------------+------------------------------------------------------|
| ~cross-platform~ | Platform-agnostic configs (zsh, Emacs, SSH, vim ...) |
| ~macos~          | macOS-specific configs                               |
| ~macos-root~     | macOS system-level configs (requires sudo)           |
| ~fedora~         | Fedora-specific configs                              |

** Emacs

I use Emacs 30.x with [[https://github.com/doomemacs/doomemacs][Doom Emacs]].
Configuration is stored in [[file:cross-platform/.doom.d][cross-platform/.doom.d]].

*** Installing Emacs

**** macOS

#+begin_src shell
brew tap d12frosted/emacs-plus && brew install emacs-plus@30
#+end_src

**** Fedora

#+begin_src shell
sudo dnf install emacs
#+end_src

*** Installing Doom Emacs

#+begin_src shell
git clone --depth 1 https://github.com/doomemacs/doomemacs ~/.emacs.d
~/.emacs.d/bin/doom install
#+end_src
