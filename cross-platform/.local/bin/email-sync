#!/usr/bin/env bash
# Sync emails with mbsync and update notmuch database.
# Can be triggered by cron:
#   */1 * * * * email-sync >/tmp/email-sync.log 2>&1
set -eo pipefail

pkill -x mbsync 2>/dev/null || true

mbsync -a || { echo "mbsync failed" >&2; exit 1; }

notmuch new
